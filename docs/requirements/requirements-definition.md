# Social Link App — 要件定義ドキュメント

最終更新: 2026-08-04（要件定義フェーズでの調査・議論の記録）

## 1. 概要

ASD等コミュニケーションに難がある人向けに、対面での会話をその場で録音・分析し、対話を支援するWebアプリケーション。場面（職場・家族・初対面等）に応じて機能を選択できることを想定。

✅**表現修正（2026-08-04）**：旧版は「リアルタイムで対話を支援する」としていたが、11章で確定した実装は「録音→停止→まとめて解析→結果表示」というバッチ方式であり、会話中に割り込んで即時介入する設計ではない（会話中の演出はブラウザ側の波形アニメーションのみ、10章確定事項6参照）。「リアルタイム」という語を外部説明にそのまま使うと、会話中の即時フィードバックを期待させ実装とズレる。なお、この「録音の区切りごとに素早く結果を返す」バッチ方式は、ASD支援の文脈では会話そのものを妨げないという点でむしろ妥当な設計と評価しており、変更が必要なのは実装ではなく説明の言葉のみ。

**開発フェーズ**: 個人開発からスタート。商用化の可能性あり。この前提がベンダー選定（非商用→商用の移行しやすさ、継続性リスク）に影響している。

## 2. 機能一覧

1. **音声からの簡易レポート生成**: 現在の話題・会話の流れ・相手の反応・関係性の距離感
2. **次の会話内容の提案**
3. **発言チェック**: 発言前にテキストで下書きを入力し「こう発言しても大丈夫か」のフィードバックを受け取る
4. **会話終了後の振り返り**: 会話内容の総括
5. **体調・気分の入力**

**データポリシー（確定、2026-08-04更新）**: 音声は処理後即時破棄。**保存操作（A-⑥）をしていない会話データは保持しない**（セッション終了後、速やかに削除）。保存操作をしたRecordは、ユーザーが削除するかアカウントを削除するまで保持し続ける。

---

## 3. 機能①：音声解析アーキテクチャ

### 3.1 設計原則：テキスト分析とプロソディ分析は別物として扱い、統合する

「プロソディ（声のトーン・抑揚・リズムからの感情推定）」と「テキスト感情分析（文字起こし後の単語からのセンチメント分析）」は別物。ASD支援という文脈では「言葉ではなく声から読み取る」ことが価値の核心なので、音響ベースの専業ベンダーで妥協しない。COTOHA・AssemblyAI・Deepgramのような「感情分析」はテキストベースであり、この用途には不十分。

**検討したが候補から除外したもの**：当初の依頼ではHume AI・Azure OpenAI Realtime API・Inworld AIの3つが軸として挙げられていた。Inworld AIはTTS（音声合成）・音声クローン・AIキャラクターとの対話構築を主目的とするプラットフォームであり、感情パラメータも「生成音声にどう感情を乗せるか」の指定用途。人間同士の会話を受動的に分析する今回の用途には設計思想から外れるため除外した。Azure OpenAI Realtime APIは4.2節の通り「単体構成」の代替案として引き続き検討対象。

**NTT CPaaS「Voice API」（2026-08-04検討・除外）**：ユーザーからの提案で調査。実態は音声メッセージ送信・IVR・コールルーティング・通話録音・音声2要素認証などを提供する「プログラマブル通話基盤」（Twilio Voice APIに近い位置づけ）であり、文字起こし・感情分析・話者識別といった音声解析AI機能は持たない。今回の用途（通信インフラではなく音声解析）には合致しないため除外。NTTグループ内でテキストベースの感情分析に近いのはCOTOHA API（3.5節で検討済み）。

### 3.2 データフロー（3層構成）

| 順序 | 担当 | 入力 | 分析内容 | 返すもの |
|---|---|---|---|---|
| 1 | STT（音声認識・話者分離） | 生の音声ストリーム | 「何を話したか」＝音の意味内容と話者の区別 | テキスト＋話者ラベル＋タイムスタンプ |
| 2 | プロソディベンダー | 話者分離済みの音声クリップ（下記3.3参照） | 「どう話したか」＝声のトーン・抑揚・強度から感情次元を推定。言葉の意味は見ない | 感情スコアの時系列 |
| 3 | LLM統合層（Claude API） | 1のテキスト＋2の感情スコア＋場面プリセット＋会話履歴＋気分入力 | 上記2つの生データを統合し人間が読める解釈を生成 | レポート・提案・警告・要約 |

**重要**: LLMは音声そのものを一度も聞かない。STTのテキストとプロソディの数値データだけを受け取り「解釈・統合」する役割。COTOHA等の別のテキスト感情分析APIを挟まない理由は、LLM自身がtranscriptを文脈込みで読解できるため、単純なセンチメント分類器より精度・柔軟性で勝るから。

### 3.3 話者分離設計パターン：「一度だけ話者分離し、話者ごとにプロソディ分析する」

どのプロソディベンダーも自前の話者分離機能の有無・精度が不透明なため、**話者分離はSTT側の出力に一本化**し、プロソディベンダーには最初から「1人分だけ」の音声を渡す。

1. STT（Azure AI Speech想定）がリアルタイム話者分離で、生の混合音声から話者ラベル＋タイムスタンプ付きテキストを返す
2. バックエンドがそのタイムスタンプで同じ音声バッファを話者ごとにスライスする（AI不要、単純な配列操作）
3. 話者ごとに分離済みの音声クリップだけをプロソディベンダーへ個別送信する
4. LLMには「話者A の発言＋感情」「話者B の発言＋感情」が正しく紐づいた状態で渡る

**限界（正直に）**: STTの話者分離精度に依存する（声質が近い場合や発言が重なる場面では精度が下がる）。相槌・割り込みが多い自然な会話では完璧ではなく、「実用上妥当な近似」と捉えるべき。これはどのプロソディベンダーを選んでも共通する制約。

### 3.4 録音まわりの技術詳細

- **ブラウザ側の音声取得**: `AudioWorklet`を使用（`ScriptProcessorNode`は非推奨）。推奨フォーマットは16kHz・16bit・モノラルPCM（Azure Speechのネイティブ推奨フォーマットと一致）。マイクアクセスはHTTPS必須（またはlocalhost）。Safariでは`start()`をユーザー操作の中で呼ぶ必要がある
- **バックエンドのファンアウトは自前実装が必要**: Azure SDKの`PushAudioInputStream`は「1つのストリームを複数コンシューマーで共有する」ことを公式サポートしていない（別スレッド書き込みで不具合報告あり）。「STTへ転送しつつ話者分離用に自分でも保持する」ファンアウトは、SDK機能ではなく単一の非同期タスクで実装する自前ロジックになる
- **Azure話者分離の精度注意**: 2024年5月GA済みで安定運用可能。ただし公式ドキュメントに「タイムスタンプは音声セグメンテーションに基づき、実際の音声タイムラインと厳密には同期しないことがある」と明記あり。`wordLevelTimestampsEnabled: true`を有効にして精度を上げることが推奨されている。「Post-Stream Refinement」という関連GA機能もあり、会話終了後の話者分離結果を精緻化できる可能性がある（要追加調査、機能④での活用余地あり）

### 3.5 STT（音声認識＋話者分離）ベンダー比較

| サービス | 日本語精度 | 話者分離 | 料金目安 | 商用 |
|---|---|---|---|---|
| **Azure AI Speech**（推奨） | 100言語以上対応 | ◎ リアルタイム対応・2024年5月GA・最大35話者。タイムスタンプ精度に注意点あり | 約$1/時間（S0従量） | 可 |
| AmiVoice（通常エンジン、日本製） | ◎ 日本語特化25年、業界別エンジンあり | 機能自体は存在（`speakerDiarization`パラメータ）が、リアルタイムストリーミング対応かバッチ限定かを一次情報で確認できず（公式マニュアルへの直接アクセスが404）。**採用時は要ベンダー確認** | 1時間¥99〜、月60分無料 | 可（国内シェアNo.1） |
| Google Cloud STT | 85言語以上 | 対応（追加課金の可能性） | $0.016/分〜 | 可 |
| AssemblyAI | 16言語以上 | 対応 | $0.00025/秒(≈$0.90/時間) | 可 |
| Deepgram | Nova-3多言語で10言語 | 対応（追加課金なし） | $200無料クレジット、従量制 | 可 |
| COTOHA API (NTT) | 210万語日本語辞書 | — | 情報が2019年時点で古い（要最新確認） | 可 |

**推奨（当初、2026-08-04時点）**: Azure AI Speech（話者分離が確認済み、大手で継続性リスクが低い）。日本語精度で不足を感じればAmiVoiceへの切替を検討（ただしリアルタイム話者分離の対応状況を先に確認すること）。

**⚠️2026-08-05に方針転換（確定事項25）**：プロソディ分析にAmiVoice ESASを採用したところ、ESASがAmiVoice自身の音声認識でしか利用できない仕様と判明したため、STTベンダーもAmiVoiceへ一本化した。Azure AI Speechの実装は非アクティブ化した状態（`STT_PROVIDER=azure`）でコードに残る。詳細は確定事項24〜28・12.3節参照。

### 3.6 プロソディ（音響感情測定）ベンダー比較・選定 — 未確定・PoC必須

| 候補 | 取得データ | 接続方式 | 料金目安 | 日本語 | 商用利用 |
|---|---|---|---|---|---|
| **Empath**（日本製） | 音声の物理特徴から気分状態を判定 | REST API。**音声要件: PCM WAVE 16bit・モノラル1ch限定・11025Hz・録音時間5秒未満**（「1リクエスト=1人の短いクリップ」前提の設計で、3.3の話者分離アーキテクチャと相性が良い） | 無料枠 月300回(保存あり)/250回(保存なし)。有料は要問い合わせ | ◎ 日本語特化、数万人規模DBで学習 | 要問い合わせ。小規模ベンダーで継続性リスクに留意 |
| **Imentiv AI** | 音声感情認識＋話者分離が標準搭載 | REST API | $19/月〜（透明・自己申告制） | **確認できず**（複数回検証を試みたが日本語対応の公開情報を発見できなかった。採用検討時は要ベンダー確認） | 可（詳細未確認） |
| **audEERING**（devAIce／openSMILE） | 音響特徴量抽出＋話者埋め込み。openSMILEは学術界標準ツールとして10年以上の実績 | 商用版devAIceはSDK/API、openSMILEは自前実装可能なOSS | Entry €35/600分、Plus €250/6,000分（約$0.045〜0.063/分） | 未確認（「あらゆる言語向けに開発された」との記述あり。音響特徴ベースのため理論上は言語非依存の可能性が高い） | 商用ライセンスあり |

**実装上の追加要件（Empath採用の場合）**: ①STT側の16kHzから11025Hzへのリサンプリングが必要、②5秒超の発話は複数クリップに分割して送る必要がある（`EmpathProvider`アダプタ内に隠蔽する設計）。

### 3.6.1 補足：プロソディ解析における母国語の重要性（2026-08-04調査）

「日本語対応」の有無は軽視できない要素であることを学術研究で確認した。

- 基準となる声の高さ自体が言語・文化で異なる（日本人女性の平均的な声の高さは300〜350Hzで世界平均200〜220Hzよりほぼ1オクターブ高いという報告あり）。感情検出は「普段の声からの逸脱」を見る仕組みのため、基準がズレると誤検出につながる
- 母語話者の方が感情認識精度が高い（in-group advantage）という研究結果があり、特に短い発話でこの差が顕著。Empathの音声要件（5秒未満のクリップ）はまさにこの条件に該当する
- 感情の「感じ方」自体が文化で異なる（例：「悲しそうな声質」の判定率が米国人70%に対し日本人50%という報告）
- **重要な発見**：「日本語話者は感情を読み取る際、言葉の内容より声のトーンに自然と注意を向ける傾向がある。英語話者は逆に言葉の内容に注意を向ける」という研究結果があり、これは本アプリの価値提示（声のトーンから読み取る）が日本語話者にとって特に価値が大きいことを示唆している。東アジア文化圏の間接的なコミュニケーションスタイル（display rules研究）とも整合する
- 一方、機械学習モデルの言語間精度は80%を超えるとの報告もあり、「日本語非対応では全く使えない」わけではない。ただし本アプリは微妙なニュアンスの検出が肝であるため、日本語チューニングの有無による精度差は軽視できない

**PoC優先順位（改訂）**：Empath・AmiVoice ESAS（いずれも日本語向けに専用チューニング済みと確認）を優先。audEERINGは学術的信頼性を理由に比較対象として維持。Imentiv AIは音声感情APIとしての日本語対応が3回の調査でも確認できなかったため（見つかった「多言語対応」は動画広告翻訳機能の話で別物）、優先度を下げ、無料枠での簡易確認に留める。

**✅稼働状況の再検証（2026-08-04）**：本アプリは既にHume AI（3.7節）・Azure Speaker Recognition（12.2節）の2件のベンダー廃止を経験しているため、契約を検討する前に4候補すべての現行稼働状況を再確認した。**4社とも廃止・撤退の兆候なし**（Empath：2026年1月時点の新製品リリースあり、稼働中。Imentiv AI：料金ページ・API稼働中。audEERING：devAIce/openSMILEとも稼働中、2026年にopenSMILE 3.0をリリース。AmiVoice：2026年にCloud Platformを全面刷新したばかりで積極投資中）。数値面での更新（公式価格ページ`acp.amivoice.com/en/amivoice_api/price/`で裏取り済み）：AmiVoiceの汎用エンジン非同期HTTP料金は1時間あたり¥79.2（ログあり）〜¥99（ログなし）で従量制（無音区間は課金対象外）。**重要な訂正**：ESAS（感情解析）はこの基本認識料金とは別建ての追加課金で、¥0.044/秒＝¥158.4/時間（月60分無料、この無料枠が基本エンジンの無料枠と共通か別かは未確認）。ESASが音声認識APIのオプション機能として提供されている以上、ESAS単体（¥158.4/時間のみ）で呼べるのか、AmiVoice側の音声認識（本アプリは既にAzure Speechで代替済み）も併せて必要になり基本料金¥79.2/時間が上乗せされるのかが未確認で、契約前に要問い合わせ。Empathの無料枠「月300回(保存あり)/250回(保存なし)」は公式プラン一覧ページ（`webempath.net/plan/info`）で直接確認し、正確な数字と確定した（ホームページ上に見つかった「3万回/月」という記述は「基本利用料で」との前置きがあり、無料枠ではなく別の有料プランを指すものと判明。誤って両者を同一の無料枠の話として比較していたのを訂正）。300回超過時の有料単価は引き続き要問い合わせ。

### 3.7 重要な訂正記録：Hume AI Expression Measurement APIについて

当初、プロソディ分析の第一候補としてHume AI Expression Measurement APIを推奨していたが、調査の過程で**2026年6月14日に完全に提供終了していたことが判明**（新規ジョブ受付は5月14日で終了）。Humeは「単体の測定API」からEVI（対話AI）への機能統合に戦略転換しており、この用途（人間同士の会話を受動的に分析する）への提供自体から撤退する方向。

また、Hume EVIは「AIと人間1対1の対話」を前提に設計されており、そもそも人間同士の会話を横で聴く受動的な用途には向いていなかった（Expression Measurement API自体にも複数話者を分離する機能は組み込まれていなかった）。

**教訓**: 音響ベースの感情測定API市場は、資金力のあるHumeでさえ撤退するほど変動が大きい。プロソディ層はどのベンダーを選んでも「将来また廃止されるかもしれない」前提で、ベンダー差し替えが容易な抽象化（`ProsodyProvider`インターフェース）を組んでおくべき。

---

## 4. アーキテクチャ全体

### 4.1 推奨構成（ハイブリッド）

| レイヤー | 推奨 | 理由 |
|---|---|---|
| 文字起こし＋話者分離＋感情分析 | AmiVoice（確定事項25で変更、当初はAzure AI Speech） | ESAS感情分析が音声認識と不可分なため一本化。Azure実装は非アクティブ化して保持 |
| 音響プロソディ・感情 | 未確定（3.6のPoCで決定） | どの候補も一長一短。話者分離をAzure側に一本化する設計にすれば、プロソディベンダー自身の話者分離機能の有無に依存しない |
| 対話理解・提案・チェック・要約 | Claude API（Sonnet 5 + Haiku 4.5） | 文脈推論・長い会話履歴の保持・日本語運用に強く、プロンプトキャッシュでコスト最適化可能 |

**リスク整理**:
- ベンダー継続性リスク：小〜中規模ベンダー（Empath、Imentiv AI等）は大手より撤退・値上げリスクが相対的に高い。商用化を見据えるならコアパスは大手で構成し、日本特化ベンダーは「精度が明確に勝る場合のみの二次候補」と位置づける
- 精度未検証リスク：どのベンダーも「実際の自然な会話」における定量精度を公表していない（公開数値は演技音声データセットでの学術ベンチマークが中心）。本格投資前のPoCは必須
- プライバシー・法務リスク：商用化を見据えるなら、選定ベンダーのデータ保持設定の確認が必要
- ✅**誤判定による心理的リスク（新規追加、2026-08-04）**：本パイプラインは話者分離→話者識別→プロソディ解析→LLM統合という複数のAI推論を直列に重ねており、各段階の誤差が積み重なる。ASD支援という用途では、「相手は実際には不快に感じていないのに、AIが誤って"距離ができている"等と判定する」ような誤判定は、支援どころか不安を増幅させうる。精度そのものの改善はPoC・継続的な検証に委ねるほかないが、**出力の見せ方でこのリスクを緩和する**方針を11章に反映した（下記）

### 4.2 Realtime単体構成⇄ハイブリッド構成の切り替え設計

**結論：可能。むしろ推奨する設計。**

Azure OpenAI Realtime API単体でも機能的には全て実現可能（何も壊れない）が、代償がある：

| 機能 | Realtime単体での実現度 | 備考 |
|---|---|---|
| ①レポート生成 | △ 精度に代償あり | 声のトーンを数値化する専用機能がなく、感情/トーンの解釈がテキスト内容からの推測に留まる。「言葉は穏やかだが声が震えている」のようなズレを検出しにくい＝**アプリの核心的価値が最も影響を受ける部分** |
| ②次の会話提案 | ◯ 問題なし | LLM推論のみで完結 |
| ③発言チェック | ◯ **完全に無関係** | 発言前チェックのため、音声パイプラインを一切使わない独立したテキスト機能 |
| ④振り返り | △ 精度に代償あり | ①と同様の理由 |
| ⑤気分入力 | ◯ 無関係 | 外部API不要 |

犠牲になるのは①④の「声から読み取る」解像度のみ。個人開発の検証フェーズではRealtime単体で素早く動くものを作り、精度が物足りなければハイブリッドへ切り替える、という進め方が現実的。

`ConversationAnalysisPipeline`という抽象インターフェースを定義し、実装を2種類（`HybridPipeline` / `RealtimeOnlyPipeline`）用意して環境変数（`PIPELINE_MODE`）で切り替える（詳細はフォルダ構成案を参照）。

---

## 5. 技術スタック（2026-08-04 最終確定）

11章で「常時ライブ解析ではなくバッチ処理で十分」と判明する前に書かれたWebSocket前提の記述が残っていたため、以下で確定・修正する。

- **フロントエンド**: Next.js (React + TypeScript) + Tailwind CSS、PWA化（対面会話中にスマホでアプリのように使えることが目的。解析は外部AIベンダー依存のためオンライン必須で、オフライン動作は前提としない）
  - ✅**新規確定（2026-08-04）**：データ取得・ポーリングにはTanStack Query（React Query）を採用。11.6の「解析結果を項目ごとにポーリングし、準備済みのものから逐次表示する」要件を`refetchInterval`でそのまま実現できる
  - ✅**新規確定（2026-08-04）**：フォーム（ログイン・新規登録・パスワード関連・声紋登録同意・設定・発言チェック・体調記録等、フォーム画面が多いため）はReact Hook Form＋Zodで統一する。11.2のパスワード要件（Unicode正規化を含む）等のバリデーションをZodスキーマとして一箇所にまとめる
- **バックエンド**: Python + FastAPI（非同期I/O、各AI SDKとの親和性、話者識別のローカルPyTorch推論（12章）をホストする余地を残す）
  - ✅**新規確定（2026-08-04）**：DBアクセスはSQLAlchemy 2.0の非同期エンジン（`asyncpg`）を基本とする。ただしCeleryワーカーはFastAPIのようなasyncioイベントループを前提としない同期実行モデルのため、ワーカー内のCeleryタスク関数（同期）から`asyncio.run(...)`で非同期の`services`/`repositories`をそのまま呼び出す方式にし、API用と別に同期専用のリポジトリ層を重複実装しない
- **通信方式（確定）**：WebSocketは不使用。11.5で確定した「アップロード→非同期ジョブで解析→ポーリングで結果取得」というジョブ実行＋REST APIの構成のみで全機能を実現する。旧版の「常時接続WebSocketで結果をpush」という記載は、11章の発見（バッチ処理で十分）以前の古い前提であり誤りだったため削除する
- **非同期ジョブキュー（新規確定）**：Celery + Redis（brokerとして）を採用する。録音解析（STT→話者識別→プロソディ→LLM統合、6章`backend/app/workers/`が担当）は数秒〜数十秒かかりうるため、HTTPリクエスト内で完結させずバックグラウンドジョブとして実行する。Celeryを選ぶ理由：リトライ・監視（Flower等）・スケールの実績が豊富で、商用化を見据えた運用に耐える（責務分離を重視する6章の方針とも合致）。個人開発フェーズの運用負荷を下げたい場合、asyncio対応の軽量ジョブキュー`arq`が代替候補になりうるが、まずはCeleryを既定として進める
  - ✅**新規確定（2026-08-04）**：Celery Beatによる定期クリーンアップジョブも追加する。2章・8章で確定済みの「A-⑥で保存されなかった会話データは速やかに削除する」方針は、ユーザーが単にタブを閉じた／アプリを離れた場合を検知するクライアント側シグナルが存在しないため、明示的な「会話終了」APIコールだけでは実現できない。一定時間（例：数時間）操作のない未保存`Conversation`を定期的に削除するバッチジョブとして実装することで、この方針を確実に満たす
- **DB**: PostgreSQL（ユーザー・会話ログ・気分記録・場面プリセット等の永続データ）、Redis（Celeryのジョブキューbroker／結果バックエンドとして使用。旧版の「セッション状態・WS管理」はWebSocket前提の誤りだったため修正）
- **認証（確定・修正）**：Auth.js / Supabase Auth等のマネージド型は不採用に変更する。理由は二つ：①バックエンドがFastAPI（Python）である以上、Next.js/JS前提のAuth.jsとは実行環境が一致しない。②11.2で確定した「メール／ユーザー名＋パスワード」「ユーザー名を忘れた場合」等の独自フローは、マジックリンクやOAuthを主眼とする多くのマネージド認証サービスが素直にはサポートしない。よってバックエンド（FastAPI）内に認証ロジックを自前実装する：
  - ✅**パスワードハッシュ化ライブラリを確定（2026-08-04）**：Argon2id（`argon2-cffi`）を採用。OWASP Password Storage Cheat Sheetが第一推奨とする方式で、bcryptより新しい設計（GPU/ASIC耐性のメモリハード関数）。次のステップ#3はこれで解消
  - セッション方式：JWTアクセストークン（短寿命）＋リフレッシュトークン（長寿命、httpOnly・Secure・SameSite=LaxなCookieで保持）。ライブラリは`PyJWT`を想定（`python-jose`は過去にCVE報告があるため避ける）。リフレッシュトークンは完全ステートレスにはせず、`RefreshToken`テーブル（11.9参照）でDB照合する方式にし、ログアウト時に確実に失効させられるようにする
  - リフレッシュトークンCookieを安全に共有するため、フロントエンドとバックエンドは同一の登録可能ドメイン配下のサブドメイン（例：`app.example.com`と`api.example.com`）での運用を推奨する。運営ドメイン自体は未確定（法務文書の運営者情報プレースホルダーと同様、事業側の決定待ち）
- **ホスティング**: フロントはVercel、バックエンドはFly.io/Railway/Azure Container Apps等。**選定理由を修正**：旧版の「常時接続WebSocketのため」は誤り。正しい理由は、Celeryワーカー（非同期ジョブ実行）や話者識別のローカルPyTorch推論（12章）を常時起動しておく必要があり、典型的なサーバーレス関数の実行時間・メモリ制約に収まらない可能性があるため。Azure AI Speech利用時は東日本リージョンでの同居も検討

## 6. フォルダ構成案（責務分離を重視した構成、2026-08-04改訂）

11章の発見（バッチ処理で十分・WebSocket不要）と、「現場の開発と同等の責務分離を行う」という方針を反映。フロントエンドの画面単位の詳細は11.2、APIエンドポイント一覧は11.3、話者識別の設計は11.5を参照。

```
social-link-app/
├── frontend/                      # Next.js（詳細は11.2）
│   ├── app/
│   ├── components/
│   ├── hooks/
│   └── lib/
├── backend/                       # FastAPI
│   ├── app/
│   │   ├── main.py
│   │   ├── core/                  # config.py / security.py / logging.py / celery_app.py（Celeryインスタンス＋Beatスケジュール定義）
│   │   ├── api/v1/                # ①プレゼンテーション層：HTTP入出力のみ、業務ロジックを持たない
│   │   │   ├── auth.py            # register/login/forgot-*/reset-password/refresh/logout
│   │   │   ├── users.py           # アカウント削除（DELETE /api/users/me）等アカウント管理
│   │   │   ├── conversations.py
│   │   │   ├── recordings.py
│   │   │   ├── voice_profile.py   # 声紋登録・状況確認・削除
│   │   │   ├── statement_check.py
│   │   │   └── records.py         # 一覧取得・削除
│   │   ├── schemas/                # Pydantic DTO（APIの入出力契約。DBモデルとは別に管理）
│   │   ├── services/               # ②ビジネスロジック層：ユースケース単位のオーケストレーション
│   │   │   ├── auth_service.py            # トークン発行・検証、RefreshToken失効管理
│   │   │   ├── conversation_service.py
│   │   │   ├── analysis_service.py       # 文字起こし→話者識別(自分/相手)→プロソディ→LLM統合を束ねる
│   │   │   ├── voice_enrollment_service.py
│   │   │   ├── statement_check_service.py
│   │   │   └── record_service.py
│   │   ├── integrations/           # ③外部連携・モデル推論のアダプタ層（上位からは共通I/Fのみ見える）
│   │   │   ├── stt/{base.py, azure_speech.py}
│   │   │   ├── prosody/{base.py, empath.py, imentiv.py, audeering.py}
│   │   │   ├── speaker_id/{base.py, ecapa_local.py}   # セルフホスト話者照合（12.1参照）
│   │   │   └── llm/claude.py
│   │   ├── repositories/           # ④データアクセス層：DBクエリのみ、業務ロジックを持たない
│   │   │   ├── user_repository.py
│   │   │   ├── refresh_token_repository.py
│   │   │   ├── conversation_repository.py
│   │   │   ├── recording_repository.py
│   │   │   ├── voice_profile_repository.py
│   │   │   └── record_repository.py
│   │   ├── models/                 # SQLAlchemy ORM（DBスキーマ定義）
│   │   ├── workers/                # Celeryタスク（同期関数から非同期services/repositoriesをasyncio.run(...)で呼び出す。5章参照）
│   │   │   ├── analysis_worker.py        # 録音解析ジョブ（音声は一時ストレージ経由で受け渡し。11.5参照）
│   │   │   └── cleanup_worker.py         # Celery Beat：未保存Conversationの定期削除、孤立した一時音声ファイルの削除
│   │   └── audio/                  # フォーマット正規化・話者ごとスライス・一時ストレージの読み書きユーティリティ
│   ├── alembic/                     # DBマイグレーション
│   └── tests/{unit, integration}
├── infra/                          # Dockerfile（backend）、docker-compose.yml（ローカル：postgres/redis/backend/worker）等。初期セットアップ時に整備
└── docs/requirements/              # 本ドキュメント
```

**依存の向き**：`api`（HTTPのみ）→`services`（業務ロジック）→`repositories`（DB）／`integrations`（外部連携・モデル推論）。上位層は下位層の実装詳細（どのベンダーか、どのORMか）を知らない構成にする。ベンダーやモデルの差し替え（プロソディ・話者識別など）は`integrations`配下の実装を追加し、環境変数で選択するだけで済む設計。

---

## 7. 機能②〜⑤の実現方針

- **②次の会話内容提案**: Claude層に①のレポート＋場面プリセットを渡して提案生成
- **③発言チェック（確定UX）**: ユーザーがテキストで発言案を入力→判定→フィードバックを返す。音声パイプラインと完全に独立した単純なREST API（`POST /api/statement-check`、WebSocket不要）。意図的な確認操作のため即時性は不要 → 判定品質を優先しSonnet 5をデフォルト推奨（速度・コスト優先ならHaiku 4.5でも可）。入力context: 発言案テキスト＋関係性/場面プリセット（会話中のチェックなら①のレポートも文脈として利用可）
- **④振り返り**: 会話終了後にSonnet 5で全トランスクリプト+プロソディ推移+気分入力を統合要約
- **⑤体調・気分入力**: 外部API不要、単純なフォーム。①④の文脈情報として活用

---

## 8. プライバシーと同意（確定事項含む、2026-08-04更新）

会話相手（第三者）の音声を録音し外部クラウドAPIに送信する設計のため、以下を明確にすべき：

- ✅**確定**：会話相手への同意は、画面上に一言添えるUI（A-③録音画面に常時表示するリマインド文言。11.1参照）で対応する。同意の可否をブロックする確認モーダルではなく、軽い注意喚起として表示
- ✅**確定**：声紋データ（12章）は「会話の相手を判別する目的にのみ使用し、それ以外の目的では利用しない」旨を、①声紋登録画面上のその場の告知、②利用規約、の両方に明記する
- 日本の個人情報保護法(APPI)上、ASD開示や気分・体調データ、声紋（生体情報に近い）は「要配慮個人情報」に該当しうる
- プライバシー・バイ・デザイン：音声は処理後即時破棄し永続保存しない（確定済み方針）。声紋は埋め込みベクトルのみ保持し生音声は保持しない
- パスワードはハッシュ化して保存（平文保存はしない。bcrypt/argon2等の適応型ハッシュを想定）
- 可能な範囲でデータレジデンシー（日本リージョン）を選択
- 本格運用前に法律専門家への相談・利用規約およびプライバシーポリシーの正式な草案作成を推奨（本ドキュメントは法的助言ではない）

---

## 9. 概算コスト試算（月100会話×平均15分＝1500分/月という仮定）

- STT (Azure Speech): 約$25/月
- プロソディ: 要PoC後再試算（Imentiv AIは$19/月〜のプラン制、Empathは無料枠内で個人開発なら収まる可能性）
- LLM (Claude、キャッシュ最適化前の概算): 約$20/月
- **合計目安: 月$65〜90程度**（プロソディ候補次第で変動。プロンプトキャッシュ活用でさらに低減余地）
- 代替案（Azure Realtime単体+LLM別途）は同等かやや高め($110〜170/月)になりうるが統合はシンプル

---

## 10. 確定事項・未決事項

### 確定事項（ユーザー回答）

1. テキスト分析とプロソディ分析は別々に行い統合する
2. 個人開発スタート、商用化の可能性あり
3. 機能③「発言チェック」は発言前・テキストベースのチェック機能
4. ✅**訂正（2026-08-04）**：音声は即破棄。会話ログの「30日保持」は撤回し、**保存操作（A-⑥）をしていない会話データは保持しない**（速やかに削除）。保存したRecordのみ、削除するまで保持する
5. UIは「場面選択」＋「①録音→停止→②解析→③結果表示」のループ＋「まとめ」＋「体調・気分記録」という一連のフロー（11章参照）。**録音は録音完了後にまとめて解析するバッチ方式**であり、会話中の常時ライブ解析ではない
6. 録音中の演出はブラウザ側の視覚効果（波形アニメーション等）のみで良く、バックエンドでのリアルタイム解析は不要
7. フッターナビゲーションは全画面で常時表示（録音中の誤操作対策として、録音中のみ移動確認ダイアログを挟む）
8. **認証方式を確定（メールマジックリンク案から変更）**：メールアドレス／ユーザー名／パスワード（ハッシュ保存）によるID・パスワード認証。ログインはメールアドレスかユーザー名のいずれか＋パスワード。「ユーザー名を忘れた場合」（メール入力→登録済みなら該当メール宛にユーザー名を通知）と「パスワードを忘れた場合」（メール入力→リセット用リンクを送付→新パスワード入力＋確認）の導線を用意。パスワード要件：8文字以上、かつ半角英字・全角英字・数字・記号のうち2種類以上を組み合わせる
9. 声紋登録は設定画面（D-①）から行える導線とする。「会話サポート」を開始しようとした時点で未登録なら声紋登録画面へリダイレクトする
10. マイク権限は**アプリ起動時にリクエスト**。その時点で許可されていない場合、録音ボタンを押した瞬間に再度リクエストする
11. 音声録音時間の上限は**30分**（⚠️確定事項28で**60秒**に変更。AmiVoice ESAS採用に伴う待ち時間対策・MVP向けの暫定値）
12. 解析中のローディング体験：予測時間の表示＋「解析中...」表示。先に確定した情報（例：話題）があれば全体の完了を待たずに逐次表示する（理想は5秒以内に何か表示）。エラー表示は実際に発生したエラー内容に応じた具体的な文言にする（詳細は11.5参照）
13. GitHubリポジトリ：https://github.com/Melting-Sugar/Social_Link_AI （ローカルで`git init`＋リモート追加済み。コミット・pushは未実施）
14. UIの配色は「モダン・パステルで温かみのある色合い・AIらしさを避ける」方向へ確定。珊瑚色（coral）と琥珀色（gold）を基調とした2トーン、フォントは丸ゴシック系（Hiragino Maru Gothic ProN / SF Rounded相当）を優先（11.6参照）
15. ✅**技術スタック・アーキテクチャを最終確定（2026-08-04）**：5章に残っていたWebSocket前提の記述（バッチ処理確定より前の古い前提）を修正。通信は「アップロード→非同期ジョブ→ポーリング」のREST構成に統一し、非同期ジョブキューはCelery＋Redis（broker）を採用。認証はAuth.js等のマネージド型ではなく、FastAPI内へ自前実装（JWTアクセス＋リフレッシュトークン、リフレッシュトークンはhttpOnly Cookie）する方式に確定
16. ✅**着手前レビューで発見した3件の抜けを解消（2026-08-04）**：①パスワードハッシュ化ライブラリをArgon2id（`argon2-cffi`）に確定（次のステップ#3を解消）。②リフレッシュトークンを完全ステートレスJWTにせず`RefreshToken`テーブル（11.9）でDB照合する方式にし、ログアウト時に確実に失効できるようにした。③「未保存の会話データを速やかに削除する」方針（2章・8章）を実際に満たす手段として、Celery Beatによる定期クリーンアップジョブの追加を明記（5章）
17. ✅**2回目の着手前レビューで発見した6件の抜けを解消（2026-08-04）**：技術スタック・フォルダ構成・APIエンドポイントを実装直前の想定で突き合わせ、以下を追加・確定した。
    - **APIエンドポイントの欠落4件**：`POST /api/auth/refresh`（アクセストークン再発行。JWT方式に必須だが一覧から漏れていた）、`POST /api/auth/logout`（D-①のログアウトに対応するAPIが存在しなかった）、`DELETE /api/records/{id}`（11.9のデータモデルの削除権限に対応するAPIが存在しなかった）、`DELETE /api/users/me`（プライバシーポリシー6条の「アカウント削除」の約束を満たすUI・APIが存在しなかった）。`DELETE /api/voice-profile`も追加（声紋のみの削除）。C-①・D-①のUIにも削除導線を追加（11.1参照）
    - **録音アップロードの受け渡し方式を新規確定**：CeleryのRedisブローカーは音声バイナリの受け渡しに不向きなため、共有一時ストレージ＋ファイルパス受け渡し方式に確定（処理後即削除、Celery Beatで孤立ファイルも掃除。11.5参照）
    - **Celeryワーカーの同期実行モデルとasync SQLAlchemyの橋渡し方法を確定**：ワーカー内で`asyncio.run(...)`により既存の非同期services/repositoriesをそのまま呼び出し、同期専用リポジトリの二重実装を避ける（5章）
    - **フロントエンドのデータ取得・フォームライブラリを新規確定**：TanStack Query（ポーリングに`refetchInterval`を利用）、React Hook Form＋Zod（5章）
    - **フォルダ構成を上記の決定に合わせて更新**：`core/celery_app.py`、`api/v1/users.py`、`repositories/refresh_token_repository.py`、`workers/`をanalysis/cleanupの2ファイルに分割（6章）
18. ✅**3回目の着手前レビュー（ユーザーからの指摘、2026-08-04）**：API構成を独立に再点検した指摘を受け、突き合わせた結果は以下の通り。
    - `POST /api/auth/logout`・`POST /api/auth/refresh`・`DELETE /api/records/{id}`・`DELETE /api/users/me`の4件は、確定事項17（同日）で**既に追加済み**であることを確認
    - `GET /api/users/me`（D-①の「アカウント情報」表示の取得元）が**未追加のまま残っていたことを新たに発見**、追加した（11.5参照）
    - **概要（1章）の「リアルタイムで対話を支援する」という表現を修正**：11章確定のバッチ方式（録音→停止→解析→表示）と語感がズレており、外部説明として使うと期待値がズレるとの指摘を受け、「対面での会話をその場で録音・分析し」に修正（1章参照）
    - **AI推定の伝え方を新規確定**：話者分離→話者識別→プロソディ→LLMという直列推論の誤差が積み重なるリスクは4.1に既述の通りだが、「支援のつもりが誤判定で不安を増幅しかねない」というASD支援特有の懸念を踏まえ、解釈が介在する出力（会話の流れ・相手の反応・関係性の距離感）を断定調から推定を示す言い回しに変更し、分析結果画面に「AIによる推定です」という一言を追加（モックアップ反映済み）。B-①発言チェックの判定文言など今後実装する画面にも同じ考え方を適用する（11.11参照）
19. ✅**プロジェクト初期セットアップ（コーディング開始、2026-08-04）**：backend/・frontend/・infra/の雛形を実装し、バックエンドは`uv run uvicorn`起動・全19エンドポイント疎通・ruffクリーン、フロントエンドは`npm run build`・lintクリーンをそれぞれ確認済み。実装の過程で以下を新たに決定・修正した。
    - **`PasswordResetToken`モデルを追加**：11.9のデータモデル一覧に無かったが、11.2確定の「使い切りのリセットトークン」を実現するには`RefreshToken`と同様にDB照合による失効管理が必要なため、実装の自然な帰結として追加（`RefreshToken`と同型：token_hash・expires_at・used_at）
    - **`GET /api/conversations/{id}`を追加**：11.5のエンドポイント一覧に無かったが、11.4の「リロードしても会話が失われない」を満たすには会話の場面（scene）等をIDから再取得する手段が必須なため追加
    - **リフレッシュトークンCookieの`path`を`/api/auth`から`/`に修正**：フロントエンドのproxy.ts（認証ガード）がこのCookieの有無を見て未ログイン判定を行うが、`path=/api/auth`のままではフロントエンドの画面リクエストにCookieが一切付与されず機能しないことが実装時に判明。`COOKIE_DOMAIN`設定も追加し、本番でのサブドメイン共有（5章）に備えた
    - **Next.js 16の破壊的変更に対応**：`middleware.ts`が`proxy.ts`（`export function proxy(...)`）に名称変更されていたため、11.4の記載を読み替えて対応。Tailwind CSSもv4のCSS-first設定（`tailwind.config.ts`ではなく`globals.css`内の`@theme`）を使用
    - **フロントエンドのAI推定表示・話者識別失敗時のエラー文言を実装**：11.11の方針をA-④・A-⑤・B-①の実装に反映。§12の自分/相手判定に失敗した場合の専用エラー文言（11.7には無かったケース）を新規に追加
    - **ローカル開発環境**：Node.js v18では動作しないため（Next.js 16の要件は`>=20.9.0`）nvmでv22を導入・既定化。Python側は`uv`を導入
    - この時点でプロソディ・話者識別（実モデル）は未接続（次のステップ#1・#2のPoC待ち。それぞれ`PROSODY_PROVIDER=none`・`SPEAKER_ID_PROVIDER=none`で動作し、話者識別が使えない場合は解析が明示的なエラーで失敗する設計）。DBマイグレーション（Alembicの初回リビジョン）は、この環境にPostgreSQLが無く生成できなかったため未実施（`docker compose up -d postgres`後に`uv run alembic revision --autogenerate`が必要）
20. ✅**実際にローカルで動かして発見・修正した3件の実行時バグ（2026-08-04、確定事項19と同日）**：Homebrewでpostgresql@17・redisを導入し、実際にDBマイグレーションを生成・適用してAPIサーバーとフロントエンドを起動、ブラウザ操作を含めE2Eで動作確認した。lint/buildでは検出できない実行時ロジックの誤りを3件発見・修正済み。
    - **通知メール送信の失敗が新規登録そのものを失敗させていた**：`auth_service.register()`が登録完了メール送信を無防備に呼び出しており、Resend APIキー未設定などでメール送信が失敗すると新規登録リクエスト全体が500エラーになっていた（DBのロールバック自体は正しく動作し、不整合データは残らないが、機能としては「登録できない」状態）。登録完了・ユーザー名再通知・パスワード再設定の3箇所とも、メール送信失敗をログに残しつつ握りつぶす方式に修正（11.2のエンドユーザー列挙対策の観点でも、メール基盤の障害でエラーの出方が変わるのは望ましくないため）
    - **`created_at`以外の日時カラムが軒並みタイムゾーン情報を持たない設定だった**：`security.py`側は一貫してタイムゾーン付き（UTC）の日時を生成しているのに対し、SQLAlchemyモデル側は`DateTime(timezone=True)`の指定漏れで大半のカラムがタイムゾーンなし設定になっており、リフレッシュトークンの最初の書き込みで例外が発生した。5つのモデルファイルを修正し、未リリースだった初回マイグレーションを作り直した
    - **同一秒内に発行したJWTが衝突しうる設計だった**：HS256の署名は決定的で、`iat`・`exp`は秒単位に丸められるため、同じユーザーに対して同一秒内に発行された2つのトークン（例：新規登録の直後にログインする、という実際にありうる操作）が完全に同一の文字列になり、`RefreshToken.token_hash`の一意制約違反を起こしていた。`security.py`の各トークン発行関数にランダムな`jti`（JWT ID）を追加して解消
    - 修正後、ブラウザ操作を含め「新規登録→ログイン→トークン更新→ログアウト→ログアウト後の更新失敗→パスワードを忘れた場合（列挙耐性込み）→アカウント削除（カスケード削除をDBで確認）」の一連の流れが実際に動作することを確認済み
21. ✅**APIキー管理方針を確定（ユーザー指示、2026-08-04）**：①APIキーはGitHubに上げない、②APIキー管理は単一のフォルダ／ファイルに集約し必ずそこから参照する（将来、運用担当者が自分自身のアカウントでキーを取得する前提のため）という2点の指示を受け、以下の構成に変更した。
    - プロジェクトルートに`.env.keys`（gitignore対象、サードパーティのAPIキーのみを集約：`ANTHROPIC_API_KEY`・`AZURE_SPEECH_KEY`・`AZURE_SPEECH_REGION`・`RESEND_API_KEY`）と、そのテンプレート`.env.keys.example`（追跡対象、取得先URL付きコメント）を新設
    - `backend/.env`・`.env.example`からはAPIキー関連の行を削除（DB接続情報やJWTシークレット等、サードパーティのアカウントに紐づかないアプリ側の設定のみを残す）
    - `backend/app/core/config.py`は`.env.keys`を`__file__`基準の絶対パスで解決して読み込む（cwdに依存させない。frontend側のlib/read-legal-doc.tsで先に対処したのと同じ問題への対処）。Docker実行時はコンテナにファイルがマウントされないため、`infra/docker-compose.yml`の各サービスに`env_file: ../.env.keys`を追加し、環境変数として注入する形で対応
    - 実際にANTHROPIC_API_KEYを設定し、`get_settings().anthropic_api_key`が正しく読み込まれることを確認済み。発言チェック（B-①）を実キーで実際に呼び出し、安全な発言・注意が必要な発言の両方で妥当な判定・言い換え案が返ることを確認した（プロンプト設計・11.11のツール定義とも正しく機能）
22. ✅**CI（GitHub Actions）を追加（2026-08-04）**：`.github/workflows/backend.yml`（postgresサービスコンテナ＋ruff＋pytest）・`frontend.yml`（Node 22＋lint＋build）。バックエンドは、これまでの手動E2E検証で見つけた3件のバグ（メール送信失敗によるブロック・タイムゾーン不整合・JWT衝突）をそのまま自動テスト化した`tests/integration/test_auth_flow.py`を新規作成し、専用のテストDB（`social_link_test`）に対して実行・全件パス確認済み。プロソディ・話者識別は引き続きPoC待ちのため、CIのテスト範囲には含めていない
23. ✅**話者識別モデルPoC：SpeechBrain ECAPA-TDNN vs pyannote-audio（2026-08-04）**：合成テスト音声（macOS `say`コマンド＋`ffmpeg`で2話者×計3クリップ、16kHz mono）を用意し、両モデルを実際に動かして比較した。
    - **SpeechBrain（ECAPA-TDNN）**：認証・アカウント登録が一切不要で、`uv sync --extra speaker-id`のみで即動作。埋め込み次元192、初回モデルロード後は1推論あたり約0.1秒。コサイン類似度は同一話者ペアで0.88、異なる話者ペアで0.20と明確に分離しており、実データでも判定に十分な精度差があることを確認した
    - **pyannote-audio（`pyannote/embedding`）**：Hugging Face Hub上で**ゲート付きモデル**であることが実行時に判明（`GatedRepoError: 401`）。利用するにはHugging Faceアカウントの作成・モデル利用規約への同意・アクセストークン（`HF_TOKEN`）の発行が必須で、開発者個人ではなくエンドユーザー環境ごとに追加のアカウント依存が生じる。アカウント作成はエージェントが代行できない操作のため、この時点で実データ比較は未実施（公開ベンチマーク値のみで比較）
    - **結論・採用**：公開ベンチマーク（SpeechBrain EER 0.69% vs pyannote-audio EER 2.8%）に加え、実動作確認でも精度・速度ともにSpeechBrainが良好だった。何より、12.3節でセルフホスト方式を選んだ本来の狙い（外部ベンダー・外部アカウントへの依存を無くすこと）に対し、pyannote-audioのゲート付きモデルは「Hugging Faceアカウントへの依存」という形で同種のリスクを再導入してしまう。この理由から**SpeechBrain（ECAPA-TDNN）を正式採用**とする。`SPEAKER_ID_PROVIDER`の既定値は引き続き`none`（軽量なPyTorch依存を必須にしないため）とし、有効化する場合は`SPEAKER_ID_PROVIDER=ecapa_local`と`uv sync --extra speaker-id`を明示的に指定する運用とする。pyannote-audio版アダプタ（`backend/app/integrations/speaker_id/pyannote_local.py`）は、将来`HF_TOKEN`を用意して第2の実データ比較をしたくなった場合に備え、切替可能な形で実装のみ残す
24. ✅**Azure AI Speech（STT）の実キーを設定・実動作確認（2026-08-04）**：ユーザーがAzure Portalでリソースを作成しキー・エンドポイント（`japaneast`）を取得、`.env.keys`の`AZURE_SPEECH_KEY`に設定。合成した日本語音声（macOS `say`コマンド）を`AzureSpeechProvider.transcribe()`（`backend/app/integrations/stt/azure_speech.py`）に実際に通し、`ConversationTranscriber`が正しく起動・文字起こし・話者ラベル付与（`Guest-1`）・セッション終了までを問題なく完了することを確認した（入力音声「こんにちは、今日はいい天気ですね。会話のテストをしています。」に対し、句読点付与も含め高精度な書き起こしを取得）。同ファイルにあった「credentials未検証」という注記は解消。これで会話サポート（A-②以降）のSTT段階は実データでの動作確認が取れた。残るは話者分離結果を実際の複数話者会話に対して確認すること（プロソディ・話者識別との統合動作は次のステップ#1のプロソディPoC後に確認予定）
25. ✅**プロソディベンダーをAmiVoice ESASに決定・実データで検証（2026-08-04、ユーザー指示）**：ユーザーがAmiVoice Cloud Platformを契約（プラン「会話_汎用」、従量課金0.025円/秒・ログ保存あり）、APIキーを`.env.keys`の`AMIVOICE_API_KEY`に設定。
    - **API仕様の重要な発見**：ESAS（感情分析）はAmiVoice自身の音声認識リクエストに`sentimentAnalysis=True`を付与する形でのみ利用可能（非同期HTTP v1限定、v2は未対応）。単体では呼べず、文字起こし・話者分離（`speakerDiarization=True`）と常にセットで返る。つまりAmiVoice ESASを使う＝AmiVoice自身をSTTベンダーとしても使うことを意味する
    - **話者分離の実データ検証**：まず合成音声（TTS2種を単純連結）でAmiVoice・Azure Speech両方をテストしたところ、**両者とも話者を正しく分離できなかった**（Azureは単一話者として返し、AmiVoiceは単語単位で話者ラベルが不規則に入れ替わった）。合成音声特有の不自然さ（間・呼吸音がない）が原因の可能性が高いと判断し、ユーザー提供の実録音（男性2名の実会話、77秒、著作権未確認のためリポジトリには一切含めない）で再検証した
    - **実録音での結果**：Azureは4セグメントに分割したが**全て同一話者ラベル**を付与し、話者分離は事実上機能しなかった。AmiVoiceは12個の話者ブロックに分割し、ユーザーが実際の会話内容と照合して正誤を判定した結果、**「司会→回答者」のような構造的な話者交代は概ね正確に検出できていたが、「知ってる」「多分」「うん」「なるほど」といった短い相槌・割り込みが直前直後の発言者に誤って吸収される**という限定的なエラーパターンが確認された。文字起こし自体にも固有名詞の誤認識等はあったが、これは話者分離の精度とは別の問題
    - **ユーザーの評価・結論**：「不完全ながら、普通のAIと同じ程度の精度。実用には耐えうる」。**AmiVoice ESASを採用**。感情分析（ESAS）自体は実データで意味のある値（時間経過に伴い変動する20種の感情パラメータ）を返すことも確認済み
    - **アーキテクチャへの影響（要実装）**：ESASが音声認識と不可分である以上、STT段階もAzureからAmiVoiceへ一本化するのが自然（二重に文字起こし料金を払う必要がなくなり、パイプラインも単純化する）。§3.3が前提としていた「STT側で話者分離→話者ごとにクリップを切り出してプロソディベンダーへ個別送信」という設計は、AmiVoiceの場合不要になる（１回のAPI呼び出しで文字起こし・話者ラベル・タイムスタンプ付き感情パラメータが全て返るため）。Azure Speech用の実装（`azure_speech.py`）はコードとして残すが非アクティブ化し、既定のSTT/プロソディベンダーをAmiVoiceに切り替える対応を次のステップとして実施する
26. ✅**AmiVoice ESAS：感情パラメータの定義・処理時間・並行実行を実データで確認（2026-08-05）**
    - **感情パラメータの正式定義**：専用API（`GET /v1/sentiment-analysis/ja/result-parameters.json`）から20パラメータの日本語名・値域を取得し確認した。ほとんどは0〜30または0〜100だが、**`emo_cog`（感情バランス論理）のみ1〜500、`atmosphere`（雰囲気・会話傾向）のみ-100〜100の符号付き**という例外があり、実データでこれらが100を超えたり負値になったりするのは仕様通りの正常な挙動（前回「emo_cogが100超で不審」としたのは誤りで訂正）。主要パラメータ：energy＝エネルギー、stress＝ストレス、concentration＝集中、anticipation＝期待、intensive_thinking＝思考、brain_power＝脳活動、excitement＝興奮、confidence＝自信、aggression＝攻撃性・憤り、upset＝動揺、content＝喜び、dissatisfaction＝不満、extreme_emotion＝極端な起伏、他
    - **処理時間の実測**：77.4秒の実会話音声を「録音完了後にファイル1本としてまとめて送信」する現行アーキテクチャ通りの方式で送信したところ、投稿から完了まで**77.2秒**（音声長とほぼ同じ、リアルタイム比約1倍）。**30分（本アプリの録音上限）の会話では解析完了まで約30分かかりうる**ことになり、§11.3で目標としていた「5秒で何か出す」という体験とは大きく乖離する。UIの進捗表示設計に影響する重要な制約として記録
    - **並行実行の実測**：処理時間短縮の可能性を検証するため、同じ音声を3件同時に投稿したところ、完了は投稿からt+71秒・t+74秒・t+96秒で、単発実行（77秒）の3倍（231秒）にはならなかった。**AmiVoiceは複数ジョブをある程度並行処理できる**ことを確認（3件目のみ約25%の遅延あり、軽微なキューイングが発生する可能性）
    - **今後の改善案（未実装・要検証）**：録音中に音声を数分単位のチャンクに分割し、区切りがつき次第順次AmiVoiceへ投稿すれば、チャンクの処理は録音継続中に並行して進み、録音停止後に待つのは最後のチャンク分（数分程度）で済み、会話全体の長さに依存しなくなる可能性が高い。ただし①今回確認した並行実行は3件までで、30分会話を3分チャンクに分けた場合の約10件同時実行でも同程度の並行性が保たれるかは未検証（レート制限の可能性）、②チャンク境界をまたぐと話者ラベル（speaker0/1）が引き継がれないため、§12の声紋照合による自分/相手判定をチャンクごとに行う実装が追加で必要、の2点が未解決。ESAS自体は非同期HTTP v1限定でストリーミング（WebSocket）には対応していないため、「録音しながら1つのセッションに継続送信する」方式は不可能で、あくまで「短いジョブを多数、順次投げる」方式でのみ改善が見込める
    - **「非同期」の意味を訂正**：ユーザーからの指摘で再確認したところ、AmiVoiceの「非同期HTTP」は「1つのジョブに音声を継続的に追記できる」という意味ではなく、「クライアントが処理完了までHTTP接続をブロックされない（投稿後すぐにsessionidが返り、別途ポーリングする）」という意味に過ぎないと公式ドキュメントで確認した。既存セッションに音声を追記する仕組みは存在せず、POST 1回＝独立した音声ファイル1本＝新しいsessionid1つ、が実際の仕様。上記の「チャンク分割」案は「1ジョブへの継続送信」ではなく「複数の独立した短いジョブを重ねて投げる」方式である点を明確化
27. ✅**Empathを実データで検証・不採用と判断（2026-08-05、ユーザー登録・実施）**：ユーザーが無料枠（月250回、保存なし）でアカウント登録しAPIキーを取得、`.env.keys`の`EMPATH_API_KEY`に設定。
    - **話者分離機能は無し**：ユーザー提供の実会話（77秒）をそのまま投げたところ`{"error":1014,"msg":"wavfile (77.332062) is too long."}`と明確に拒否された。Empathは複数話者の音声を分離する機能を持たず、呼び出す側が事前に1人分・5秒未満のクリップを切り出しておく必要がある設計（§3.6の想定通りだったことを実データで確認）
    - **処理方式はAmiVoiceと対照的**：エンドポイントは`POST https://api.webempath.net/v2/analyzeWav`で、ジョブ登録・ポーリングが一切不要な**完全に同期的なAPI**。実測でリクエストから0.4〜0.6秒でレスポンスが直接返り、AmiVoiceの「1x速度の非同期バッチ処理」とは対照的に、発話クリップが切り出され次第その場で即座に結果を取得できる
    - **感情スコアの精度**：実会話から切り出した3クリップ（0:00-0:03「世の中にはね...」／0:36-0:40「13とBを同じ文字にしている絶対違う。」／0:52-0:56「でも絶対違うって言いましたよね俺聞こえてました。」）を分析した結果、`calm`/`anger`/`joy`/`sorrow`/`energy`の値はクリップごとに明確に変動した（一定値を返しているわけではない）ものの、内容的に特段悲しい発言ではない冒頭の説明部分が`sorrow=40, calm=2`という強く偏った値を示すなど、実際の発話内容・トーンとの対応関係に疑問が残る結果だった
    - **ユーザーの評価・結論**：「Empathは使い物になる精度をしていない。複数話者の音声を分離する機能を持たないのも問題」。**Empathは不採用**とする
28. ✅**録音中チャンク並行投稿の実現可能性を追加検証・MVPアーキテクチャを最終決定（2026-08-05）**
    - **時間差投稿での再検証**：前回の「8件同時バースト投稿」という検証方法自体に疑問（実際の録音では各チャンクは録音進行に合わせて時間差で準備されるはず）が挙がり、160秒の会話を想定し20秒チャンクを実際の録音ペース通り20秒間隔で順次投稿するテストを実施。結果、チャンク1（t+0s投稿）は完了までに**156.8秒**かかり、チャンク1〜6がほぼ同時刻（t+157〜159s）にまとめて完了するという、並行処理ではなくキューイングに近い挙動が観測された。録音停止に相当する最終チャンク（t+140s投稿）も完了まで約60秒を要した。ただし直前のバーストテストの残留負荷が影響した可能性があり、クリーンな条件での再測定はできていない
    - **結論**：現状のAmiVoiceアカウントで「録音中にチャンク分割して並行投稿し待ち時間を短縮する」方式（次のステップ#2で検討していた案）を安全に採用できる根拠は得られなかった。同時実行数・レート制限の正確な仕様はAmiVoiceへの直接問い合わせが必要（次のステップに追加）
    - **MVPアーキテクチャの決定（ユーザー承認）**：チャンク分割方式は選択肢として設計・コードには残しつつ、**当面はシンプルな「録音停止後にファイル1本をAmiVoiceへ一括送信」方式を採用**。この方式でも待ち時間を実用範囲に収めるため、**録音上限時間を30分から60秒に短縮**する（バックエンド`max_recording_seconds`・フロントエンド`MAX_RECORDING_SECONDS`の両方）。単発リクエストの実測（77秒→77秒、20秒→20秒相当）はリアルタイム比1倍程度で安定していたため、60秒の録音であれば単独実行時は概ね60秒程度の待ち時間に収まる見込み（複数ユーザーが同時に使う場合の挙動は別途検証が必要）
29. ✅**パイプライン全体のコードレビューと8件の修正（2026-08-06/07）**：`AnalysisService`・`ClaudeClient`を中心に実装を見直し、以下を修正した。
    - **話者が1人しか分離できなかった場合の処理**：従来は`other_reaction`等をLLMに強制生成させていた（相手が実際には何も発言していないのに反応を捏造しうる、実際にAzure/AmiVoiceどちらでも観測済みの失敗モード）。声紋照合でその1人が自分か相手かを判別し、`self_only`/`other_only`という正直な結果を返す設計に変更（ユーザー提案）
    - **声紋照合の信頼度しきい値を追加**：`SPEAKER_ID_MIN_SIMILARITY`（絶対的な下限）・`SPEAKER_ID_MIN_MARGIN`（1位と2位の差）を新設（`.env`で調整可能）。デフォルト値は暫定（合成音声でのテストでは異なる話者同士が0.89という高い類似度を示すことが判明し、合成音声はこの閾値の検証に不向きと判明——実音声での検証はユーザー自身が行う予定）
    - **場面・プロソディスコアをLLMに渡す際の情報不足を解消**：場面は英語enum値のまま渡していたのを日本語ラベルに変更、プロソディスコアは素のPython dict文字列だったのを日本語名・値域付きの整形済みテキストに変更（値域はAmiVoice公式APIから取得済みのものを使用）
    - **想定外のエラーが生の英語例外文字列のままユーザーに表示される問題を解消**：`TimeoutError`・`httpx.HTTPError`・`anthropic.APIError`・その他未知の例外を分けて捕捉し、既存の日本語エラー文言に振り分け
    - **ラウンド間の文脈連携を追加**：直前ラウンドの話題・流れ・相手の反応・関係性の距離感を参考情報としてLLMに渡す（今回の判定を上書きしないよう明示）
    - **話者識別モデルの再ロードコストを解消**：`get_speaker_id_provider()`はCeleryタスクごとに新しいインスタンスを生成するが、モデルはインスタンス単位ではなくモジュール単位でキャッシュするよう変更。実測でタスクあたり約2.4秒→約0.2秒に短縮（`pyannote_local.py`にも同じ修正を適用）
    - **Celeryタスクにタイムアウトの二重の安全網を追加**：`task_soft_time_limit`/`task_time_limit`を新設（AmiVoice側のポーリングタイムアウト600秒だけに頼らない設計に）
    - 応答時間の内訳を実測：60秒の録音でAmiVoice呼び出しが支配的（60〜85秒、原因不明の変動あり）、Sonnet 5のレポート生成は約12秒、Haiku 4.5の話題抽出は約1.5秒
30. ✅**AmiVoiceのログ保存設定を修正、応答時間変動の追加調査（2026-08-08）**
    - **ログ保存の見落としを修正**：AmiVoice非同期HTTPは既定でログ（音声データ＋認識結果）を保存する仕様と公式ドキュメントで確認したが、`amivoice.py`の実装は`loggingOptOut`を指定しておらず、**§8の「生音声は保存しない」という方針に反してベンダー側に音声・文字起こしが保存され続けていた**。`d`パラメータに`loggingOptOut=True`を追加して修正、実際のAPI呼び出しで文字起こし・話者分離・感情分析が引き続き正常に動作することを確認済み。なお、2022年1月にAmiVoice公式が非同期APIのログ無効化に関するマニュアルのサンプルコード誤りを訂正した告知があり、この設定は歴史的にも間違えやすい箇所だったと分かった
    - **応答時間変動の追加調査**：並行負荷が原因という以前の推測（確定事項28）を検証するため、同一の20秒クリップを完全に独立して（並行実行なし）2回投げ直したところ、59.3秒・61.8秒と、並行負荷が無くてもリアルタイム比約3倍という結果になった。**並行負荷が原因という説は否定された**。真因（AmiVoice側の負荷状況・時間帯・アカウント状態等）は特定できておらず、引き続きAmiVoiceへの問い合わせが必要（次のステップ参照）
    - **支離滅裂/情報過少な会話でのレポート生成を確認**：意図的に噛み合わない文字起こしと、極端に情報の少ない文字起こしの2パターンで`generate_conversation_report`を実行。いずれも§11.11のヘッジ表現方針（「〜ように見えます」等）が適切に機能し、情報不足を正直に認め、`relationship_distance`も安全側（distant/no_change）に倒れる結果だった。プロンプトの追加調整は現時点で不要と判断

### 残る未決事項

1. 「場面」プリセットの中身は5項目（学校・大学／職場／初対面／友人との会話／恋愛・気になる人）に確定。プロンプトへの反映方法は実装時に詰める
2. STTの分岐点：AmiVoiceのリアルタイム話者分離の対応状況の検証（**ただし11章の発見によりバッチ方式で十分なため、優先度は下がった**）
3. プロソディベンダーの確定：Empath / Imentiv AI / audEERING / AmiVoice ESAS（バッチ前提が確定したため再度候補入り）の比較PoC
4. 「関係性の距離感」「次に話すと良いこと」のenum・文言・アイコンの最終確定（11.4のドラフトを参照。モックアップでは例として反映済み）
5. 利用規約・プライバシーポリシーは草案作成済み（`frontend/content/legal/`。作成時点では`docs/legal/`にあったが、Vercel/Cloudflare等frontend単体をビルド対象とするデプロイ先でも読み込めるよう2026-08-13に移動）。運営者情報・未成年者の利用可否等プレースホルダーの確定と専門家レビューが残課題
6. 解析にかかる実時間の見込み（プロソディ・LLMベンダー確定後に実測が必要。現状の「5秒で何か出す」目標は仮のUX目標）

---

## 11. UI設計（画面一覧、2026-08-04改訂）

スマホ利用を前提とし、PCでも崩れないレスポンシブなWebアプリ。デザインはモダン・パステルで温かみのある色合いとし、「AIで作った感じ」を避ける。UIモックアップ（Artifact）を作成済み — 9画面を確認できる。

### 11.1 画面一覧

| ID | 画面名 | 内容 | 遷移 |
|---|---|---|---|
| L-① | ログイン | メールアドレス or ユーザー名＋パスワード。「ユーザー名を忘れた場合」「パスワードを忘れた場合」リンクあり | 認証後A-①へ |
| L-② | 新規登録 | メールアドレス・ユーザー名・パスワード・パスワード（確認） | 登録後A-①へ |
| L-③ | ユーザー名を忘れた | メールアドレス入力→登録済みなら該当メールへユーザー名を通知 | — |
| L-④ | パスワードを忘れた | メールアドレス入力→リセット用リンクをメール送付 | メール内リンク→L-⑤ |
| L-⑤ | パスワード再設定 | 新パスワード＋確認入力 | 完了後ログインへ |
| E-① | 声紋登録 | 音声を10秒程度録音し声紋を登録。データの用途に関する同意文言を明示。**設定画面から遷移可能**。「会話サポート」開始時に未登録ならここへ自動リダイレクト | 完了後、遷移元へ戻る |
| A-① | タイトル | アプリ名「Social Link AI」＋「サポートを開始する」ボタン | →A-②（声紋未登録ならE-①経由） |
| A-② | 場面選択 | 学校・大学／職場／初対面／友人との会話／恋愛・気になる人の5項目。選択した場面は以降の画面上部に常時表示 | →A-③ |
| A-③ | 録音 | ボタン押下で録音開始→再押下で録音終了・解析開始。**同意リマインド文言と録音上限60秒を画面に表示**（確定事項28でMVP向けに30分から短縮） | →解析中画面へ |
| （解析中） | 解析中 | 予測時間表示＋「解析中...」。確定済みの情報から逐次表示（プログレッシブ表示） | 完了後A-④へ自動遷移 |
| A-④ | 分析結果 | 現在の話題・会話の流れ・相手の反応・関係性の距離感（アイコン＋定型文の5段階）・次に話すと良いこと（カテゴリ＋短文）を表示 | 「もう一度録音する」→A-③／「会話を終了する」→A-⑤ |
| A-⑤ | まとめ | 会話全体（全録音ラウンド累積）の振り返りを箇条書きで表示：今日の会話のまとめ／うまくいったポイント／次回へのアドバイス | →A-⑥ |
| A-⑥ | 記録 | 体調（4段階アイコン）、気分・不安度（0〜10のシークバー）、次回目標（任意）、メモ（任意）を入力し「記録する」でA-⑤の内容と合わせてDB保存 | 保存後は完了扱い（footerから他画面へ） |
| B-① | 発言チェック | テキストで発言案を入力＋場面/相手を選択→判定結果（大丈夫か、ダメなら言い換え案）を表示 | 独立、音声パイプライン不使用 |
| C-① | 記録一覧 | 保存済みRecordの一覧を閲覧。✅**各項目に削除操作を追加（2026-08-04）**（`DELETE /api/records/{id}`、11.5参照） | — |
| D-① | 設定 | **声紋の登録状況（未登録／登録済みのステータス表示＋登録・再登録・削除導線）**、アカウント情報、利用規約・プライバシー、ログアウト、✅**アカウント削除（2026-08-04追加）**（確認ダイアログを挟んだ上で`DELETE /api/users/me`。プライバシーポリシー6条の「アカウント削除で関連データも削除できる」という約束に対応する導線がなかったため追加） | 声紋登録行→E-① |

**フッターナビゲーション**：「会話サポート」(→A-②)／「発言チェック」(→B-①)／「今までの記録」(→C-①)／「設定」(→D-①) の4アイコン。全画面で常時表示。録音中（A-③のrecording state）にフッター操作された場合のみ、「移動すると今の録音は破棄されます」の確認ダイアログを挟む。

### 11.2 認証仕様（確定）

- 項目：メールアドレス／ユーザー名／パスワード（**ハッシュ化して保存**。bcrypt/argon2等の適応型ハッシュ関数を想定）
- ログイン：メールアドレス**または**ユーザー名＋パスワード
- パスワード要件：8文字以上、かつ「半角英字・全角英字・数字・記号」のうち2種類以上を組み合わせる。全角文字を許容するため、比較前にUnicode正規化（NFKC等）を行い表記揺れによる認証失敗を防ぐ実装上の配慮が必要
- 「ユーザー名を忘れた場合」：メールアドレスを入力→**登録済みの場合のみ**該当メール宛にユーザー名を通知。画面上に「一致するアカウントが存在すればユーザー名を送信しました」のように表示し、メール登録の有無を画面上で区別しない（アカウント存在の推測＝emailエヌメレーション対策）
- 「パスワードを忘れた場合」：メールアドレスを入力→署名付き・有効期限付き・使い切りのリセットトークンを含むリンクをメール送信→リンク先で新パスワード＋確認を入力
- **新規登録完了時**：登録時点でのメールアドレスの実在確認（確認リンク等）は挟まない。登録完了後、登録したメールアドレス宛に「登録完了メール」を送信し、本文にはユーザー名のみを記載する
- ✅**確定（2026-08-04）**：メールアドレスの実在確認は、`User.email_verified_at`（nullable）を持たせ、初回のパスワードリセットが成功した時点で暗黙的に「実在確認済み」として記録する方式で進める（新たな確認メールを追加送信せず、既存のリセット導線を流用）。未確認のままの場合は設定画面に軽い注意表示を出す程度に留め、登録や利用をブロックしない

### 11.3 声紋登録・マイク権限・録音の運用ルール（確定）

- 声紋登録はD-①設定画面からいつでも実行・再登録可能。加えて「会話サポート」（A-②以降）を開始しようとした時点で未登録ならE-①へ自動的にリダイレクトする
- 登録画面には「登録した声の情報は、会話の相手を判別する目的にのみ使用し、それ以外の目的では利用しない」旨をその場で明示し、利用規約への同リンクも設置する
- マイク権限は**アプリ起動時にリクエスト**する。その時点で許可されなかった場合は、録音ボタンを押した瞬間に再度リクエストする
- 会話相手への同意は、A-③録音画面に常時表示するリマインド文言（例：「相手にも録音・分析についてひと言伝えましょう」）で対応する。ブロッキングな確認は行わない
- 音声録音時間の上限は**60秒**（確定事項28でMVP向けに30分から短縮。AmiVoiceの単発リクエストがリアルタイム比1倍程度で安定する範囲に収めるための暫定値）。上限に達した場合は自動的に録音を終了し解析へ進む

### 11.4 フロントエンド構成（Next.js App Router）

```
frontend/app/
├── login/page.tsx                  # L-①
├── register/page.tsx               # L-②
├── forgot-username/page.tsx        # L-③
├── forgot-password/page.tsx        # L-④
├── reset-password/page.tsx         # L-⑤（メール内リンクからアクセス、トークンをクエリ等で受け取る）
├── voice-enroll/page.tsx           # E-①
├── layout.tsx                      # ルートレイアウト。フッターを常時表示（未認証時は非表示）
├── page.tsx                        # A-①
├── scene/page.tsx                  # A-②
├── conversation/[id]/
│   ├── page.tsx                    # A-③→解析中→A-④（内部stateで切替、画面遷移はしない）
│   ├── summary/page.tsx            # A-⑤
│   └── log/page.tsx                # A-⑥
├── check/page.tsx                  # B-①
├── history/page.tsx                # C-①
└── settings/page.tsx               # D-①
middleware.ts                        # 未認証アクセスを/loginへ、声紋未登録での会話サポートアクセスを/voice-enrollへリダイレクト
```

- 場面選択（A-②）で会話開始すると`POST /api/conversations`を呼び、返ってきた`conversation_id`で`/conversation/[id]`へ遷移。以降の画面はこのIDを軸にサーバーから状態取得するため、リロードしても会話が失われない
- 録音中の波形アニメーション・経過時間表示はWeb Audio APIの`AnalyserNode`でブラウザ内完結（バックエンドへは送らない）。音声アップロードは`MediaRecorder`を使用
- iPhone Safari（〜18.3はmp4/AAC）とChrome（webm/opus）で出力フォーマットが異なるため、バックエンド側で受信後にffmpeg等でWAVへ正規化してからAzure/プロソディベンダーへ渡す

### 11.5 バックエンド／APIエンドポイント

| エンドポイント | 用途 | 備考 |
|---|---|---|
| `POST /api/auth/register` | 新規登録 | L-② |
| `POST /api/auth/login` | ログイン | L-① |
| `POST /api/auth/forgot-username` | ユーザー名再通知 | L-③。存在有無を画面上で区別しない実装にする |
| `POST /api/auth/forgot-password` | パスワードリセットメール送信 | L-④ |
| `POST /api/auth/reset-password` | 新パスワード設定 | L-⑤。トークン検証必須 |
| ✅`POST /api/auth/refresh` | アクセストークン再発行 | **新規追加（2026-08-04）**。5章のJWTアクセス＋リフレッシュトークン方式には必須だが、旧版のエンドポイント一覧から漏れていた。リフレッシュトークンCookieを検証し（`RefreshToken`テーブルで失効チェック）、新しいアクセストークンを発行する |
| ✅`POST /api/auth/logout` | ログアウト | **新規追加（2026-08-04）**。D-①の「ログアウト」に対応するAPIが存在しなかった。`RefreshToken`を`revoked_at`セットで失効させ、Cookieを削除する |
| ✅`GET /api/users/me` | アカウント情報取得 | **新規追加（2026-08-04、3回目のレビューで発見）**。D-①の「アカウント情報」表示に対応する取得元が定義されていなかった。email・username・created_at等を返す |
| `POST /api/voice-profile` | 声紋登録・再登録 | E-①。音声から埋め込み抽出→`VoiceProfile`保存、生音声は保持しない |
| `GET /api/voice-profile` | 声紋登録状況の確認 | D-①のステータス表示、及びmiddlewareのリダイレクト判定に使用 |
| ✅`DELETE /api/voice-profile` | 声紋データの削除 | **新規追加（2026-08-04）**。プライバシーポリシー6条「開示・訂正・削除を求めることができる」を満たすために必要（アカウントは維持したまま声紋のみ削除するケース） |
| `POST /api/conversations` | 場面を指定して会話セッション作成 | A-② |
| `POST /api/conversations/{id}/recordings` | 録音音声をアップロードし解析をキック | A-③停止時（または60秒到達時、確定事項28）。アップロードされた音声は共有一時ストレージに保存し（下記「録音アップロードの受け渡し」参照）、Celeryジョブとして非同期実行：ffmpegで正規化→AmiVoiceバッチ文字起こし（`speakerDiarization=True`・`sentimentAnalysis=True`、確定事項25）→話者分離結果と声紋を照合し自分/相手を判定→AmiVoiceが同一呼び出しで返した感情パラメータ（ESAS）を相手speakerの発話区間分だけ集計→Claudeで統合→一時ファイルを削除。Azure＋別プロソディベンダーという旧構成は非アクティブ化した実装として残る（`STT_PROVIDER=azure`で切替可） |
| `GET /api/conversations/{id}/recordings/{rid}` | 解析結果のポーリング取得 | 解析中/A-④表示用。**段階的なフィールド（例：`topic_ready`, `flow_ready`, `report_ready`等）を持たせ、確定した項目から順次返せる設計にする**（11.6参照） |
| `POST /api/conversations/{id}/summary` | 会話全体（全ラウンド累積）の振り返り生成 | A-⑤ |
| `POST /api/conversations/{id}/log` | 体調・気分・目標・メモを保存 | A-⑥。A-⑤の内容と合わせて`Record`として日付付きでDB保存 |
| `POST /api/statement-check` | 発言案＋場面/相手のコンテキストを判定 | B-①。音声パイプライン非依存、同期処理でよい |
| `GET /api/records` | 保存済み記録の一覧取得 | C-① |
| ✅`DELETE /api/records/{id}` | 保存済み記録の削除 | **新規追加（2026-08-04）**。11.9のデータモデルで「ユーザーが削除するまで保持」と定義されていたが、対応するエンドポイントが一覧から漏れていた。C-①に削除導線を追加する |
| ✅`DELETE /api/users/me` | アカウント削除 | **新規追加（2026-08-04）**。プライバシーポリシー6条「アカウントを削除することで、関連するデータを削除できる」という約束に対し、UI（D-①設定）・APIのどちらにも対応する仕組みが存在しなかった。D-①に「アカウント削除」導線を追加し（11.1参照）、実行時に`VoiceProfile`・`Conversation`・`Recording`・`Record`・`RefreshToken`を連動削除する |

**録音アップロードの受け渡し（新規確定、2026-08-04）**：CeleryのブローカーはRedisだが、Redisは数十MBになりうる音声バイナリの受け渡しには不向き（メッセージサイズ・メモリ効率の両面で）。そのためAPIプロセスとCeleryワーカーの間で音声バイナリそのものをジョブ引数として渡すのではなく、①APIが受け取った音声を共有の一時ストレージ（個人開発フェーズではAPIとワーカーを同一ホスト上に配置し、共有ローカルディスクの一時ディレクトリを使用）に書き込み、②Celeryジョブにはファイルパスと`recording_id`だけを渡し、③ワーカーが処理完了後（成功・失敗を問わず`finally`で）ファイルを削除する。加えてCeleryBeatの定期クリーンアップ（5章）で一定時間以上残った孤立ファイルも削除し、確実に「生音声を残さない」方針（8章）を満たす。将来ワーカーを複数ホストへ水平スケールする場合は、この一時ストレージをS3互換オブジェクトストレージ（有効期限付きライフサイクルポリシー併用）に置き換える。

**アーキテクチャ上の大きな発見（既知）**：UI仕様（録音→停止→解析）から、常時ライブストリーミングではなく録音完了後のバッチ処理で十分と判明。Azureの**バッチ文字起こしAPI**（`diarizationEnabled: true`）で足りることを確認済み。副次効果として、AmiVoiceのESAS（感情解析、非同期のみ）が再びプロソディ候補に入る。

### 11.6 解析中のローディング体験（プログレッシブ表示）

「解析予測時間の表示」と「確定した情報から逐次表示（理想は5秒以内に何か出す）」を実現するため、解析結果ポーリングAPIは単一の完了フラグではなく、**項目ごとの準備状況を持つ設計**にする（例：文字起こし・話題抽出は比較的早く終わるため先に返し、プロソディ解析やLLM統合が必要な項目は後から埋まる）。フロントエンドは準備済みの項目をすぐ表示し、未準備の項目はスケルトン（プレースホルダー）表示にする。予測時間は当面ヒューリスティック（録音時間から概算）とし、ベンダーPoCで実測後にキャリブレーションする。

### 11.7 エラーハンドリング方針

エラー表示は汎用メッセージではなく、実際に発生したエラー内容に応じた具体的な文言にする。想定される主なケース：

- 無音・音声未検出：「音声が検出できませんでした。マイクの位置を確認し、もう一度お試しください。」
- ネットワークエラー：「通信エラーが発生しました。電波の良い場所でもう一度お試しください。」
- 解析ベンダー側の失敗・タイムアウト：「解析中に問題が発生しました。もう一度お試しください。」
- 録音時間上限（60秒、確定事項28）到達：「録音時間の上限（60秒）に達したため、自動的に解析を開始します。」

### 11.8 デザイントークン（v3確定、モックアップ反映済み）

配色は当初「深いティール×琥珀」の落ち着いた案を出したが、ユーザーからのフィードバックで「白〜クリーム基調＋黒文字＋パステルは装飾のみ」の方向に確定した（v2→v3）。

- **色（v4、2026-08-04再々調整）**：背景は白に寄せた薄いクリーム色（`--bg: #fbf7f0`、カードは`--surface: #ffffff`）。文字は黒に近い色（`--ink: #221f1b`、ごくわずかに灰色寄り）で統一。
  - **経緯**：v3で「coral(#ef7a61)・gold(#e7a23d)を装飾に使う」としたが、これらは彩度が高く赤みの強い色で、実際には「パステル」と呼べる淡さではなかった（「画面が赤っぽい」というフィードバックを受けて判明）。パステルは本来「明度が高く淡い」色であり、彩度を落としただけの「くすんだ濃い色」とは異なる、という区別が抜けていた
  - **v4での修正**：`coral`（#f6c3b1系）・`gold`（#f3da9c系）を、実際に明度の高い真のパステルトーンへ変更。同時に、パステルは非常に淡いため白文字では視認性が確保できないことから、**装飾要素（場面カードの塗り・ボタン・チップ・関係性の距離感の可視化等）の上に乗るテキスト・アイコンもすべて黒に近い`--ink`に統一**した（従来は白文字や`coral-strong`等の色文字を使っていた箇所を含む）。結果として「背景・文字は白〜黒の無彩色、彩度のある色は装飾の“塗り”としてのみ使う」という一貫したルールになった
  - リンクテキスト（「パスワードを忘れた場合」等）は色ではなく下線で識別できるようにし、色に依存しない設計とした
- **フォント**：日本語は代替できないため、代わりに丸ゴシック系を優先指定：`ui-rounded, "Hiragino Maru Gothic ProN", "Yu Gothic Medium", "Noto Sans JP", sans-serif`。ユーザーから高評価を得た（2026-08-04「丸ゴシック系フォントは素晴らしいです」）ため、この方針を維持する
- **関係性の距離感の視覚表現**：アプリ名「Link」にちなみ、2つの点と接続線の距離・太さで5段階を表現（凡例付き）
- 生成AIにありがちな配色（紫グラデーション、クリーム×テラコッタ×セリフ体等）は引き続き避けている

### 11.10 メール送信基盤の比較（2026-08-04調査）

パスワードリセット・ユーザー名再通知メールの送信に使用。価格・特徴を比較した結果を以下にまとめる。

| サービス | 無料枠 | 有料（5万通/月時） | 特徴 |
|---|---|---|---|
| **Resend** | 3,000通/月 | $20/月 | 開発者体験が良いと評価される。個人開発・スタートアップ向き |
| SendGrid | 100通/日（≈3,000通/月） | $19.95/月 | エンタープライズ・Twilioエコシステム向き |
| Postmark | なし（$15/月〜1万通） | $55/月 | 到達率(deliverability)の評価が最も高いが、無料枠なし・高コスト |
| AWS SES | 従量課金（$0.10/1,000通） | 約$5/月 | 圧倒的に安いが、送信ドメイン認証等の設定にAWS運用知識が必要 |

**推奨：Resend**。本サービスの送信量（パスワードリセット・ユーザー名再通知のみ、低頻度）は無料枠（3,000通/月）で長期間まかなえる見込みで、開発者体験の良さもこのフェーズに合致する。AWS SESは大規模になれば最安だが、Azure中心の構成に新たにAWSの認証・運用を持ち込むオーバーヘッドが見合わない。将来的に送信量が増えても、Resendの有料プランは十分に現実的な価格帯。

### 11.9 データモデル（ドラフト、更新）

- `User`: id, email, username, password_hash, email_verified_at（nullable。11.2参照）, created_at
- ✅**`RefreshToken`（新規追加、2026-08-04）**: id, user_id(FK), token_hash（トークンそのものは保存せずハッシュのみ）, expires_at, revoked_at（nullable）, created_at。ログアウト時やパスワード変更時にDB照合で確実に失効させるため、完全ステートレスなJWTリフレッシュトークンにはしない（5章参照）
- `VoiceProfile`: id, user_id(FK), embedding(声紋の埋め込みベクトル), enrolled_at（12章参照。**生音声は保存せず埋め込みのみ保持**）
- `Conversation`: id, user_id(FK), scene(enum), started_at, ended_at。**A-⑥で保存されないまま終了した場合、速やかに削除する**（30日保持は撤回、8章参照）
- `Recording`: id, conversation_id(FK), round番号, created_at, duration_sec（上限60秒、確定事項28）, topic, flow, other_reaction, relationship_distance(enum), suggestion_category(enum), suggestion_text（**生音声は保存しない**）。`Conversation`削除に連動して削除
- `Record`: id, conversation_id(FK), user_id(FK), date, condition(enum 4段階), mood_anxiety_score(0-10), next_goal(任意), memo(任意), summary_bullets（A-⑤の内容）。**ユーザーが削除するかアカウントを削除するまで保持**

**関係性の距離感（ドラフト、モックアップに反映済み）**：①距離ができている ②変化なし ③少しずつ打ち解けている ④やや近づいている ⑤心を開いて話せている

**次に話すと良いこと（ドラフト、モックアップに反映済み）**：質問する／共感を示す／自分の話をする／話題を変える／今は聞き役に徹する

### 11.11 出力文言の方針（新規確定、2026-08-04）

4.1で整理した「誤判定による心理的リスク」への対策として、AIの解釈・推測が入る出力は、断定ではなく推定であることが伝わる文言にする。

- **対象**：①のレポートのうち「会話の流れ」「相手の反応」「関係性の距離感」（＝プロソディ・LLMの解釈が介在する項目）。「現在の話題」（文字起こしからの抽出で断定してよい）と「次に話すと良いこと」（もともと提案・助言として書かれ、断定調ではない）は対象外
- **UI上の対応（モックアップ反映済み）**：分析結果画面（A-④）の上部に「AIによる推定です。実際の状況と異なる場合があります。」等の一言を常時表示。加えて上記3項目の文言自体も、言い切り（「〜になっています」「〜が増えています」）から推定を示す言い回し（「〜ように見えます」等）へ変更
- **今後の適用範囲**：この方針はA-④に限らず、同様にAIの解釈が介在する出力すべてに適用する——A-⑤（振り返り）の要約、B-①（発言チェック）の「大丈夫か／ダメか」の判定（まだモックアップ未作成、11.1参照）。特にB-①は「大丈夫」という誤った太鼓判が実際の対人関係で悪影響を及ぼしうるため、実装時は特に慎重な文言設計が必要
- 利用規約第3条の「分析結果の正確性を保証しない」旨の記載と一貫した方針だが、法的な免責文言だけでは利用の瞬間には効かないため、UI側の文言でも同じ考え方を表現する

---

## 12. 話者識別（自分／相手の判定）— 2026-08-04追加

### 12.1 課題

STTの話者分離（diarization）は「話者1」「話者2」のように音響的な違いで音声をクラスタリングするだけで、**どちらがアプリの利用者（自分）でどちらが会話相手かという意味はわからない**。機能①のレポート（相手の反応・関係性の距離感）は「自分から見た相手」という前提で成立するため、取り違えるとレポート内容が逆転してしまう重大な問題になりうる。

### 12.2 検討した方式と決定

| 方式 | 仕組み | 決定 |
|---|---|---|
| A. 声紋登録＋自動判定 | 初回に自分の声を登録し、以降は話者分離結果と照合して自動判定 | **採用** |
| B. 毎回手動タグ付け | 会話ごとに音声を聴いて自分の声を選択 | 不採用（毎回手間がかかる） |
| C. ハイブリッド | 初回の手動タグ付けをそのまま声紋登録として流用 | 不採用（Aとほぼ同義だが専用登録画面の方が体験として明確） |

**⚠️重要な訂正記録（第2のベンダー廃止）**：当初、声紋照合にはAzure AI Speaker Recognition APIを想定していたが、確認したところ**2025年9月30日に完全に廃止済み**（Microsoftから後継APIの提供なし）と判明。Hume AI Expression Measurement APIの廃止（3.7節）に続き、この案件で2件目の「調査時点では存在した商用APIが実際には廃止されていた」ケース。話者認識・声紋照合という領域は商用APIの供給が不安定という教訓を踏まえ、方針を変更した。

### 12.3 採用方式：セルフホストの話者埋め込みモデル

外部ベンダーに依存せず、**バックエンド内でオープンソースの話者埋め込みモデルをローカル推論する**方式を採用する。ベンダー廃止リスクが原理的に発生しない。

- ✅**PoC完了・採用モデル確定（2026-08-04、確定事項23参照）**：**SpeechBrain（ECAPA-TDNNモデル）**を正式採用。VoxCelebベンチマークでEER 0.69%（比較したpyannote-audioは2.8%）に加え、実データ（合成音声）でも同一話者0.88／異なる話者0.20とコサイン類似度が明確に分離することを確認済み。認証・外部アカウントが一切不要な点も、12.3節でセルフホスト方式を選んだ「外部ベンダー依存を無くす」という狙いと合致する。pyannote-audioは埋め込みモデル（`pyannote/embedding`）がHugging Face上でゲート付きのため見送り、アダプタ実装のみ`pyannote_local`として残す（`SPEAKER_ID_PROVIDER=pyannote_local`＋`HF_TOKEN`で切替可能）
- フロー：①初回登録時に自分の声を約10〜20秒録音→埋め込みベクトルを抽出し`VoiceProfile`として保存（生音声は保存しない）。②各会話の解析時、STTの話者分離結果（話者1/話者2）それぞれから埋め込みを抽出→登録済みの自分の埋め込みとコサイン類似度で照合→類似度が高い方を「自分」、他方を「相手」とラベル付け
- インフラ面の留意点：PyTorchベースのモデルをバックエンドで動かすため、APIのみの構成よりやや余裕を持ったCPU/メモリのホスティングを選定する必要がある
- UI追加：初回ログイン後（または「会話サポート」を初めて利用する際）に声紋登録画面を挟む。未登録のままでは会話サポート機能（A-②以降）を開始できない設計とする（B-①発言チェック・C-①記録・D-①設定は声紋不要のため利用可能）

---

## 13. 次のステップ

1. 利用規約・プライバシーポリシーの草案の内容確認・専門家レビュー・運営者情報等プレースホルダーの穴埋め（`frontend/content/legal/`参照）
2. `RESEND_API_KEY`の取得・設定（`.env.keys`参照。優先度低め — 未設定でもメール送信のみ黙って失敗し他機能は動作する設計、確定事項20参照）
3. **AmiVoiceへの問い合わせ**：応答時間の変動（並行負荷が原因ではないと確定事項30で確認済み。60〜85秒、時に約3倍速の遅延の真因）を特定するため、直接問い合わせが必要
4. 上記に関連したタイムアウト設計の見直し（現状：AmiVoice側ポーリング600秒、Celery `task_time_limit` 900秒。ユーザー体験としてどの時点で「遅い」と伝えるべきかは未検討）
5. 現在のプロンプト（`claude.py`）をチェックし、レポート生成の質が上がるよう最適化（ユーザー自身が確認予定。確定事項30で支離滅裂/情報過少なケースは動作確認済み）
6. 細かい機能・UIチェック（ユーザー自身が確認予定）

### 選択肢として保留（設計・コードは残すが未採用）

- **チャンク分割による録音中並行投稿（2026-08-05、優先度を明確に格下げ）**：当初はAmiVoiceアカウントの同時実行数・レート制限が不明なため保留としていたが、録音上限を60秒に短縮した時点でそもそも分割する動機が薄いという指摘（ユーザー）を受け方針を明確化した。60秒程度の音声であれば単発リクエストの待ち時間は実用範囲に収まっており、チャンクに分割すると①各チャンクの前後文脈が失われ文字起こし・話者分離の精度が落ちる、②チャンクごとに声紋照合による自分/相手判定をやり直し、結果をつなぎ直すフェーズが余分に必要になる、というデメリットの方が大きい。**録音上限を将来再び伸ばす場合にのみ再検討する**（その際はAmiVoiceへの同時実行数・レート制限の問い合わせが前提になる）

### 完了済み（次のステップから移動）

- ✅プロソディベンダーPoC（Empath・AmiVoice ESASを実データで検証。Empath不採用・AmiVoice ESAS採用）→ 確定事項25・26・27参照
- ✅話者識別モデルのPoC（SpeechBrain ECAPA-TDNN vs pyannote-audio）→ 確定事項23・12.3節参照
- ✅ローカルDB環境の用意とAlembic初回マイグレーション生成（Homebrew版PostgreSQL 17で実施済み）→ 確定事項20参照
- ✅CI設定（GitHub Actions）→ 確定事項22参照
- ✅`AZURE_SPEECH_KEY`の取得・設定、STTパイプラインの実動作確認 → 確定事項24参照

### 完了済み（2026-08-04）

- UIデザインの方向性確定（v3：白〜クリーム基調＋黒文字＋パステル装飾、丸ゴシックフォント）→ Artifactとして公開・更新済み
- 認証仕様・声紋登録の導線・マイク権限タイミング・同意UI・録音上限・エラーハンドリング方針の確定
- メール送信基盤の比較・Resendを推奨として選定（11.10参照）
- 利用規約・プライバシーポリシーの草案作成（`docs/legal/terms-of-service.md`, `docs/legal/privacy-policy.md`。専門家レビュー前提のドラフト）
- Gitリポジトリの初期化とリモート設定
- **技術スタック・アーキテクチャの最終確定**（5章参照）：WebSocket前提だった通信方式の記述をREST＋非同期ジョブポーリングに修正、非同期ジョブキュー（Celery＋Redis）を新規確定、認証方式をマネージド型からFastAPI自前実装＋JWT（アクセス＋リフレッシュトークン）に修正、ホスティング選定理由を修正
- **着手前レビューで発見した3件の抜けを解消**：パスワードハッシュ化ライブラリ（Argon2id）確定、リフレッシュトークンの失効管理（`RefreshToken`テーブル）追加、未保存会話データの削除を実現する定期クリーンアップジョブ（Celery Beat）追加
- **初回コミット・push実施（2026-08-04）**：要件定義・法務ドラフト一式をリポジトリにコミットし、リモート（GitHub）へpush済み
- **2回目の着手前レビューで発見した6件の抜けを解消**：認証系の欠落エンドポイント（refresh/logout）、データ削除系の欠落エンドポイント（records/{id}・users/me・voice-profile）とUI導線、録音アップロードの一時ストレージ受け渡し方式、Celeryワーカーの非同期処理橋渡し方法、フロントエンドのデータ取得・フォームライブラリを確定（詳細は確定事項17参照）
- **3回目の着手前レビュー（ユーザー指摘への対応）**：`GET /api/users/me`の欠落を新規発見・追加。概要（1章）の「リアルタイム」表現をバッチ方式に即した表現へ修正。AI推定の伝え方の方針を新規確定し、モックアップの分析結果画面に反映（詳細は確定事項18・11.11参照）
- **プロジェクトの初期セットアップ（コーディング開始）**：`backend/`（FastAPI、全19エンドポイント・モデル・Celeryワーカー実装済み、`uv run uvicorn`起動確認・ruffクリーン）、`frontend/`（Next.js 16、L-①〜⑤・E-①・A-①〜⑥・B-①・C-①・D-①の全画面実装済み、`npm run build`・lintクリーン、モックアップの配色トークンをTailwind CSS v4の`@theme`として移植）、`infra/docker-compose.yml`（postgres/redis/api/worker/beat）を実装。実装時に見つかった追加の決定事項は確定事項19を参照。プロソディ・話者識別の実ベンダー/モデル接続とDBマイグレーション生成は次のステップ#1・#2・#4として残る
